<x-layouts.admin title="Statistik">
    <div class="flex flex-wrap items-end justify-between gap-4"><div><h1 class="text-3xl font-bold tracking-tight">Statistik konten</h1><p class="mt-2 text-slate-600">Aktivitas baca agregat tanpa menyimpan identitas pribadi yang tidak diperlukan.</p></div><form class="flex flex-wrap items-end gap-2" method="GET"><label class="text-xs font-bold text-slate-600">Dari<input class="mt-1 block rounded-lg border border-slate-300 px-3 py-2" type="date" name="from" value="{{ request('from',$report['from']->toDateString()) }}"></label><label class="text-xs font-bold text-slate-600">Sampai<input class="mt-1 block rounded-lg border border-slate-300 px-3 py-2" type="date" name="to" value="{{ request('to',$report['to']->toDateString()) }}"></label><button class="rounded-lg bg-slate-950 px-4 py-2.5 text-sm font-bold text-white">Terapkan</button></form></div>
    <div class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-6">@foreach(['views'=>'Dibaca','unique_views'=>'Pembaca unik','downloads'=>'Unduhan','duration_seconds'=>'Durasi (detik)','searches'=>'Pencarian','no_results'=>'Tanpa hasil'] as $key=>$label)<article class="rounded-xl border border-slate-200 bg-white p-4"><p class="text-xs font-semibold text-slate-500">{{ $label }}</p><strong class="mt-2 block text-2xl">{{ number_format($report['metrics'][$key]) }}</strong></article>@endforeach</div>
    <div class="mt-6 grid gap-6 xl:grid-cols-[2fr_1fr]"><section class="rounded-xl border border-slate-200 bg-white p-5"><h2 class="font-bold">Tren pembacaan</h2><div class="mt-4 h-72"><canvas data-stat-chart data-labels='@json($report["daily"]->pluck("label"))' data-values='@json($report["daily"]->pluck("views"))' aria-label="Grafik pembacaan" role="img"></canvas></div></section><section class="rounded-xl border border-slate-200 bg-white p-5"><h2 class="font-bold">Perangkat</h2><div class="mt-4 h-72"><canvas data-device-chart data-labels='@json($report["devices"]->keys())' data-values='@json($report["devices"]->values())' aria-label="Grafik perangkat" role="img"></canvas></div></section></div>
    <section class="mt-6 rounded-xl border border-slate-200 bg-white p-5"><div class="flex flex-wrap items-center justify-between gap-3"><div><h2 class="font-bold">Buku terpopuler</h2><p class="mt-1 text-sm text-slate-500">Diurutkan berdasarkan pembacaan dan unduhan pada periode terpilih.</p></div><div class="flex gap-2">@foreach(['csv'=>'CSV','xlsx'=>'XLSX','pdf'=>'PDF'] as $format=>$label)<a class="rounded-lg border border-slate-300 px-3 py-2 text-xs font-bold" href="{{ route('admin.statistics.export',[...request()->only('from','to'),'format'=>$format]) }}">{{ $label }}</a>@endforeach</div></div><div class="mt-4 overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b border-slate-200 text-xs text-slate-500"><tr><th class="py-3">Judul</th><th>Dibaca</th><th>Diunduh</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($report['popularBooks'] as $book)<tr><td class="py-3 font-semibold">{{ $book->title }}</td><td>{{ number_format($book->views_count) }}</td><td>{{ number_format($book->downloads_count) }}</td></tr>@endforeach</tbody></table></div></section>
</x-layouts.admin>
