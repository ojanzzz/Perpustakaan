<x-layouts.admin title="Audit log">
    <div class="flex flex-wrap items-end justify-between gap-4"><div><h1 class="text-3xl font-bold tracking-tight">Audit log</h1><p class="mt-2 text-slate-600">Rekaman aktivitas permanen dan hanya-baca.</p></div><a href="{{ route('admin.audit.export', request()->query()) }}" class="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-bold">Ekspor CSV</a></div>
    <form class="mt-6 grid gap-3 rounded-xl border border-slate-200 bg-white p-4 sm:grid-cols-4"><input name="action" value="{{ $filters['action'] ?? '' }}" placeholder="Awalan aksi" class="rounded-lg border-slate-300"><input type="date" name="from" value="{{ $filters['from'] ?? '' }}" class="rounded-lg border-slate-300"><input type="date" name="to" value="{{ $filters['to'] ?? '' }}" class="rounded-lg border-slate-300"><button class="rounded-lg bg-slate-950 px-4 py-2 font-bold text-white">Filter</button></form>
    <div class="mt-6 overflow-x-auto rounded-xl border border-slate-200 bg-white"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="p-4">Waktu</th><th>Aktor</th><th>Aksi</th><th>Target</th><th>IP</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($logs as $log)<tr><td class="whitespace-nowrap p-4">{{ $log->created_at?->format('d M Y H:i:s') }}</td><td>{{ $log->user?->email ?? 'Sistem/Publik' }}</td><td class="font-mono text-xs font-semibold">{{ $log->action }}</td><td class="text-xs">{{ class_basename($log->target_type ?? '') }}{{ $log->target_id ? '#'.$log->target_id : '' }}</td><td class="text-xs text-slate-500">{{ $log->ip_address ?: '—' }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-slate-500">Belum ada aktivitas pada filter ini.</td></tr>@endforelse</tbody></table></div>
    <div class="mt-6">{{ $logs->links() }}</div>
</x-layouts.admin>
