<x-layouts.member title="Notifikasi" heading="Notifikasi" description="Pembaruan koleksi dan kategori yang Anda langgani.">
    <div class="member-list">@forelse($notifications as $notification)<article @class(['is-unread'=>$notification->unread()])><div><strong>{{ $notification->data['title'] ?? 'Pemberitahuan baru' }}</strong><p>{{ $notification->data['message'] ?? 'Ada pembaruan pada perpustakaan digital.' }}</p><small>{{ $notification->created_at->diffForHumans() }}</small></div><div class="flex gap-2">@if(!empty($notification->data['url']))<a class="button-secondary" href="{{ $notification->data['url'] }}">Buka</a>@endif @if($notification->unread())<form method="POST" action="{{ route('member.notifications.read',$notification) }}">@csrf @method('PUT')<button class="button-secondary">Tandai dibaca</button></form>@endif</div></article>@empty<x-public.empty-state title="Belum ada notifikasi" description="Langgani kategori untuk menerima informasi buku baru." />@endforelse</div><div class="mt-8">{{ $notifications->links() }}</div>
</x-layouts.member>
